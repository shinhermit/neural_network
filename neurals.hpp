#ifndef NEURALS_HPP
#define NEURALS_HPP

namespace neurals
{
  const short THRESHOLD = 0.5; /*!< activation limit (seuil)*/
  const float UNEVALUATED = -1; /*!< output value before any evalaution*/
  const double RND_MIN = -0.5; /*!< min value for random weights init*/
  const double RND_MAX = 0.5; /*!< max value for random weights init*/
};

#endif
